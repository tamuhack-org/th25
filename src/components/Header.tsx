import TrustBadge from './TrustBadge';

const Header = () => {
    return (
        <header>
            <a href="#">logo</a>
            <TrustBadge />
        </header>
    );
};

export default Header;
